<h1>Wszystkie wydarzenia</h1>

<ul>
  <% for event in @events %>
    <li><%= link_to event.artist, "/events/#{event.id}" %> <%= event.event_date < Date.today ? 'Archive' : '' %></li>
  <% end %>
</ul>
<% if user_signed_in? && current_user.admin %>
  <%= link_to 'Nowe ogłoszenie', new_event_path %>
<% end %>